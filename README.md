# illuminaticonfirmed

A basic React app deployed to GitHub Pages at `https://winkwinknation.github.io/illuminaticonfirmed`.

## Setup

1. `npm install`
2. `npm start`

## Deploy

1. `npm run deploy`

The `homepage` is set to `https://winkwinknation.github.io/illuminaticonfirmed`.
